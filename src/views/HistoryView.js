import React, { Component } from 'react'
import { Text, StyleSheet, View } from 'react-native'

export default class HistoryView extends Component {
    render() {
        return (
            <View>
                <Text> Hifistoforyfy </Text>
            </View>
        )
    }
}

const styles = StyleSheet.create({})
